package binaryTree.hufuman_exec1;
/**
 * @className: HuffumanUnzipExec
 * @description:   
 * @author SunHang
 * @createTime 2021/4/12 13:11
 */

import java.util.Map;

/**
 * 实现解压缩的功能
 * 1. 读取压缩后的文件与编码表
 * 2. 将赫夫曼编码后的byte[]重新转为二进制的字符串
 * 3. 将编码表重新恢复为HashMap
 * 4. 将二进制字符串，对照赫夫曼编码表进行解压缩
 * 5. 将解压缩后的文件保存起来。
 */
public class HuffumanUnzipExec {

    /**
     *
     * @param huffmanCodes 赫夫曼编码表
     * @param huffmanBytes 编码后对应的字节数组
     * @return 原来字符串对应的数组
     */
//    private static byte[] decode(Map<Byte,String> huffmanCodes, byte[] huffmanBytes) {
//
//    }
    /**
     *
     * @param flag 标志是否需要进行补高位操作
     * @param b 传入的 byte
     * @return b 对应的二进制字符串
     */
//    private static String byteToBitString(boolean flag, byte b) {
//
//    }
}
